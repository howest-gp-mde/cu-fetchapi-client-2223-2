<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:Mde.FetchClient.ViewModels" 
             x:DataType="viewmodels:LoginViewModel"
             x:Class="Mde.FetchClient.Pages.LoginPage">
    <ContentPage.Content>
        <StackLayout>
            <Entry Placeholder="your handle" Text="{Binding UserName}" />

            <Entry Placeholder="your password" Text="{Binding Password}" IsPassword="True" />

            <Button Text="Login" Command="{Binding LoginCommand}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>